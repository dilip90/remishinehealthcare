export const metadata = {
  title: 'Admin CMS - Remishine Healthcare',
  description: 'Remishine Healthcare content management panel.',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
